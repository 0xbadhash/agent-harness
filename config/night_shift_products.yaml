# Products for overnight readiness (03:15 HKT = 19:15 UTC).
# Format: product_id: path
# Paths MUST be portable: use ~/... or $HOME-relative only (expanduser applied).
# Do NOT commit /home/<user>/... absolute paths (hardcode scan will fail).
# Override file: $NIGHT_SHIFT_PRODUCTS_FILE

watchlist: ~/watchlist
email-detach: ~/email-detach
substack-push: ~/substack-push
second-brain: ~/second-brain
catalyxt: ~/catalyxt-website
agent-harness: ~/agent-harness
ocr-ledger: ~/ocr-ledger
zk-business-card: ~/zk-business-card
bip39lab: ~/bip39lab
# product_id must match product_plugin product_id / project_label (figure-it-out)
figure-it-out: ~/figure-it-out
