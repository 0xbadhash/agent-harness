# Plan — P0–P1 feedback loops

## Ticket order

1. `finish_ship.py` + tests + ship-flow  
2. `promote_night_fails.py` + stubs option + tests  
3. `portfolio_install_report.py` + tests  
4. `remaining_board.py` + release_mgmt/ship-flow pointer  

## Shared

All write under `.agents/artifacts/`. Portable; no host secrets.
